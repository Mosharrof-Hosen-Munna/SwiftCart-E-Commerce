1. What is the difference between null and undefined?
ans: null basically indicate no value in any variables or nothing and undifined means no value are not difined into variale as value

2 What is the use of the map() function in JavaScript? How is it different from forEach()?
ans: map function in javascript create a new array by doing some changes or functionalities of map function block return new array by modify or before elements, and other side forEach function only execute block code and nothing return as value

3.What is the difference between == and ===?
ans: == is not check type of value but === is check type of value or varibles then compare eachother.

4. What is the significance of async/await in fetching API data?
ans: JavaScript is single-threaded language so async await use for non blocking Execution 

5.Explain the concept of Scope in JavaScript (Global, Function, Block)
ans: Scope determines where variables are accessible in your code: Global is available everywhere, Function is restricted to its specific function, and Block (defined by {}) limits let and const to that specific block. It acts as a set of rules that prevents variable name collisions and ensures data security by controlling visibility.